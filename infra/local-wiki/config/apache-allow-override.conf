# Allow .htaccess overrides for the MediaWiki docroot so the pretty-URL
# rewrite (config/htaccess) can take effect. Mirrors prod, where the
# shared-hosting Apache reads .htaccess directly. Mounted into
# /etc/apache2/conf-enabled/ via docker-compose.
<Directory /var/www/html>
    AllowOverride All
</Directory>
